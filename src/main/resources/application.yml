spring:
  datasource:
    url: jdbc:postgresql://172.18.0.1:2731/postgres
    username: postgres
    password: postgres
  jpa:
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate.ddl-auto: create-drop

app:
  version: ${version}
  name: ${name}
